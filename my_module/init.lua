-- This is the only file that is ran when `require("my_module")` is called, so if you have things split up you should return a table with each submodule

-- local my_module = {}
-- my_module.submodule1 = require("my_module.submodule1")
-- my_module.submodule2 = require("my_module.submodule2")
-- return my_module
